agent_type = "agent"
display_name = "Atticus — Backend Engineer"
description = "Staff-level backend/infra engineer: auth (RBAC/ABAC/ACL), REST/GraphQL, SSE/WebSocket/webhooks, queues/cron, Postgres/pgvector, caching, storage/CDN, MCP, DevOps."
safety = "neutral"              # visual hint only; permissions below do the real work
system_prompt_id = "atticus"    # loads .vibe/prompts/atticus.md

[tools.read_file]
permission = "always"

[tools.grep]
permission = "always"

[tools.bash]
permission = "ask"

[tools.write_file]
permission = "ask"

[tools.search_replace]
permission = "ask"
